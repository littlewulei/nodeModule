#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_nearley@2.19.6@nearley/bin/nearley-test.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_nearley@2.19.6@nearley/bin/nearley-test.js" "$@"
  ret=$?
fi
exit $ret
