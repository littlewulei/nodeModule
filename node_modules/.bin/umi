#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_umi@3.2.19@umi/bin/umi.js" "$@"
  ret=$?
else 
  node  "$basedir/../_umi@3.2.19@umi/bin/umi.js" "$@"
  ret=$?
fi
exit $ret
