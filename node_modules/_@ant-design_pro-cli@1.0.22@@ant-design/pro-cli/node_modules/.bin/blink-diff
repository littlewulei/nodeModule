#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_blink-diff@1.0.13@blink-diff/bin/blink-diff" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_blink-diff@1.0.13@blink-diff/bin/blink-diff" "$@"
  ret=$?
fi
exit $ret
